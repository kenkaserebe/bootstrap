This folder creates a storage account and container to store our state files.

The commands to make this happen are:
terraform init
terraform plan
terraform apply --auto-approve

To delete this infrastructure use:
terraform destroy --auto-approve